diff --git a/src/ServerScriptService/Server/Duels/DuelService.luau b/src/ServerScriptService/Server/Duels/DuelService.luau
--- a/src/ServerScriptService/Server/Duels/DuelService.luau
+++ b/src/ServerScriptService/Server/Duels/DuelService.luau
@@
 function DuelService.new(
 	store: Store,
 	bus: Bus,
 	characters: CharacterServiceApi,
-	stance: StanceServiceApi
+	_stance: StanceServiceApi
 ): Service
@@
-		if state.stance ~= "Inactive" then
+		-- O jogador escolhe entrar em modo de luta com a tecla 1.
+		-- O duelo só pode nascer depois que a animação Start terminou.
+		if state.stance ~= "Active" then
 			return false
 		end
@@
-		local stateA =
-			store.get(fighterA)
-
-		if stateA ~= nil
-			and stateA.stance ~= "Inactive"
-		then
-			stance.deactivate(fighterA)
-		end
-
-		local stateB =
-			store.get(fighterB)
-
-		if stateB ~= nil
-			and stateB.stance ~= "Inactive"
-		then
-			stance.deactivate(fighterB)
-		end
-
 		record.history.add(
 			"DuelEnded",
 			nil,
@@
-		local stanceAStarted =
-			stance.start(aUserId)
-
-		local stanceBStarted =
-			stance.start(bUserId)
-
-		if not stanceAStarted
-			or not stanceBStarted
-		then
-			endDuelById(
-				duelId,
-				"InvalidState"
-			)
-
-			return nil
-		end
-
 		bus.publish(
 			EventTypes.create(
 				"DuelCreated",
@@
 		if stateA.duelId ~= duel.id
 			or stateB.duelId ~= duel.id
 		then
 			return "InvalidState"
 		end
+
+		if stateA.stance ~= "Active"
+			or stateB.stance ~= "Active"
+		then
+			return "InvalidState"
+		end

diff --git a/src/ServerScriptService/Server/Duels/DuelEngine.luau b/src/ServerScriptService/Server/Duels/DuelEngine.luau
--- a/src/ServerScriptService/Server/Duels/DuelEngine.luau
+++ b/src/ServerScriptService/Server/Duels/DuelEngine.luau
@@
-			stanceActivated = false,
+			-- DuelService só cria a sessão quando os dois jogadores
+			-- já terminaram a entrada de stance.
+			stanceActivated = true,

diff --git a/src/StarterPlayer/StarterCharacterScripts/CharacterPresentation.client.luau b/src/StarterPlayer/StarterCharacterScripts/CharacterPresentation.client.luau
--- a/src/StarterPlayer/StarterCharacterScripts/CharacterPresentation.client.luau
+++ b/src/StarterPlayer/StarterCharacterScripts/CharacterPresentation.client.luau
@@
 local ReplicatedStorage =
 	game:GetService("ReplicatedStorage")

-
-local player =
-	Players.LocalPlayer
-
 local characterInstance =
 	script.Parent
